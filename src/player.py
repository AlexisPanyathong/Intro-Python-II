# Write a class to hold player information, e.g. what room they are in
# currently.
class Player:
    def __init__(self, cur_name, cur_room):
        self.cur_name = cur_name
        self.cur_room = cur_room